                sum.append(matrix[j][i])
                a=max(sum)
            for j in range(len(matrix)):
        for i in range(len(matrix[0])):
        
        
            result.append(min(h))
            sum=[]
            result.append(a)
        for y in range(len(result)):